{
  "devDependencies": {
    "lerna": "2.0.0-beta.28"
  },
  "scripts": {
  	"setup": "lerna bootstrap && lerna run build",
  	"test": "lerna run test",
  	"cover-publish": "lerna run cover-publish"
  }
}
