{
  "name": "bash-tokenizer",
  "version": "0.0.0",
  "description": "Standard compliant bash tokenizer",
  "repository": "vorpal-js/bash-tokenizer",
  "license": "MIT",
  "author": "vorpal-js",
  "scripts": {
    "test": "ava --no-babel && xo --ignore grammar.js",
    "build": "jison src/grammar.jison > grammar.js"
  },
  "keywords": [],
  "engines": {
    "node": ">=5"
  },
  "devDependencies": {
    "ava": "^0.13.0",
    "jison": "^0.4.17",
    "xo": "^0.13.0"
  },
  "files": [
    "src",
    "./grammar.js"
  ],
  "dependencies": {
    "compose-function": "^3.0.3"
  }
}
